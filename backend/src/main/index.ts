/**
 * Application entrypoint (composition root).
 *
 * ADR-022:
 * - Wire dependencies and start transport/runtime concerns here.
 * - Keep domain/application logic out of this layer.
 */

export {};
